services:
  hygie:
    image: ghcr.io/carryozor/hygie:2.5.1
    container_name: hygie
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Europe/Paris
      # Encrypt API keys at rest — recommended for production.
      # 1. Copy .env.example to .env and fill in HYGIE_ENCRYPTION_KEY
      # 2. Generate a key: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
      # Never hardcode the key here — use the .env file instead.
      - HYGIE_ENCRYPTION_KEY=${HYGIE_ENCRYPTION_KEY:-}
      # Optional: set DATABASE_URL to use MariaDB instead of SQLite (requires mariadb profile)
      - DATABASE_URL=${DATABASE_URL:-}
    mem_limit: 1g     # 512m is too tight for large libraries; 1-2g recommended in production
    cpus: '2.0'
    healthcheck:
      test: ["CMD", "python3", "/app/backend/healthcheck.py"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    depends_on:
      mariadb:
        condition: service_healthy
        required: false

  mariadb:
    image: mariadb:11
    profiles: [mariadb]
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: hygie
      MYSQL_USER: hygie
      MYSQL_PASSWORD: ${DB_MARIADB_PASSWORD:-hygie_secret}
      MYSQL_ROOT_PASSWORD: ${DB_MARIADB_ROOT_PASSWORD:-root_secret}
    volumes:
      - hygie_mariadb:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost", "-u", "hygie", "-phygie_secret"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  hygie_mariadb:
