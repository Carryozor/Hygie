# v2.8 — Expert Rules & Observability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un système de règles expert (style Maintainerr) avec modèles Pydantic structurés, normaliser le schéma media_queue, améliorer l'observabilité par bibliothèque et les tests d'intégration.

**Architecture:** Les règles expertes reposent sur des modèles Pydantic (`backend/rules/models.py`) sérialisés en JSON dans SQLite. Un moteur d'évaluation pur Python (`backend/rules/engine.py`) décide par item si la règle s'applique. Le scanner appelle ce moteur après les règles simples existantes. L'UI actuelle (vanilla JS) expose une interface de configuration simple ; le rule-builder visuel drag-and-drop viendra en v3.0 avec Vue 3.

**Tech Stack:** Python 3.11+, Pydantic v2, aiosqlite, FastAPI, pytest-asyncio. Aucune nouvelle dépendance externe.

---

## Fichiers créés / modifiés

| Fichier | Action | Responsabilité |
|---------|--------|---------------|
| `backend/rules/__init__.py` | Créer | Package rules |
| `backend/rules/models.py` | Créer | Pydantic models: Condition, ExpertRule, RuleAction, RuleOperator |
| `backend/rules/engine.py` | Créer | evaluate_condition(), evaluate_rule() |
| `backend/routers/expert_rules.py` | Créer | CRUD endpoints + /preview |
| `backend/db/schema.py` | Modifier | Table expert_rules + migration notified_* |
| `backend/db/repositories.py` | Modifier | CRUD expert_rules, get_affected_items() |
| `backend/db/__init__.py` | Modifier | Exports nouveaux repositories |
| `backend/scanner.py` | Modifier | Intégrer évaluation expert rules dans run_scan |
| `backend/main.py` | Modifier | include_router(expert_rules.router) |
| `frontend/static/js/app.js` | Modifier | UI configuration expert rules (basique) |
| `tests/test_rules_models.py` | Créer | Tests Pydantic models + validation |
| `tests/test_rules_engine.py` | Créer | Tests unitaires evaluate_condition/evaluate_rule |
| `tests/test_expert_rules_api.py` | Créer | Tests API CRUD + preview |
| `tests/test_scan_integration.py` | Créer | Test intégration run_scan avec SQLite réel |
| `tests/test_deletion_integration.py` | Créer | Test intégration run_deletion avec SQLite réel |
| `backend/version.py` | Modifier | 2.8.0 |

---

## Task 1 — Pydantic models pour les règles expertes

**Files:**
- Create: `backend/rules/__init__.py`
- Create: `backend/rules/models.py`
- Create: `tests/test_rules_models.py`

- [ ] **Step 1: Créer le package**

```python
# backend/rules/__init__.py
from .models import Condition, ExpertRule, RuleAction, RuleOperator, ConditionField, ConditionOp
from .engine import evaluate_rule, evaluate_condition

__all__ = [
    "Condition", "ExpertRule", "RuleAction", "RuleOperator",
    "ConditionField", "ConditionOp",
    "evaluate_rule", "evaluate_condition",
]
```

- [ ] **Step 2: Écrire le test d'abord**

```python
# tests/test_rules_models.py
import pytest
from pydantic import ValidationError
from backend.rules.models import (
    Condition, ExpertRule, RuleAction, RuleOperator,
    ConditionField, ConditionOp,
)

def test_condition_valid():
    c = Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=365)
    assert c.field == ConditionField.DAYS_NOT_WATCHED
    assert c.value == 365

def test_condition_invalid_op_for_list_field():
    # op=IN requires a list value
    with pytest.raises(ValidationError):
        Condition(field=ConditionField.SEERR_USER_ID, op=ConditionOp.IN, value=42)

def test_expert_rule_defaults():
    rule = ExpertRule(
        name="Old movies",
        conditions=[Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=365)],
    )
    assert rule.operator == RuleOperator.AND
    assert rule.action == RuleAction.QUEUE
    assert rule.enabled is True
    assert rule.priority == 0

def test_expert_rule_serialization():
    rule = ExpertRule(
        name="Old movies",
        conditions=[Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=365)],
    )
    data = rule.model_dump_json()
    restored = ExpertRule.model_validate_json(data)
    assert restored.name == rule.name
    assert len(restored.conditions) == 1

def test_condition_in_op_requires_list():
    c = Condition(field=ConditionField.SEERR_USER_ID, op=ConditionOp.IN, value=[1, 2, 3])
    assert isinstance(c.value, list)

def test_rule_with_multiple_conditions():
    rule = ExpertRule(
        name="Unwatched low-rated",
        operator=RuleOperator.AND,
        conditions=[
            Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=180),
            Condition(field=ConditionField.RATING, op=ConditionOp.LT, value=5.0),
        ],
    )
    assert len(rule.conditions) == 2
```

- [ ] **Step 3: Vérifier que le test échoue**

```bash
cd /opt/claude/hygie && python3 -m pytest tests/test_rules_models.py -v
```
Attendu : `ModuleNotFoundError: No module named 'backend.rules'`

- [ ] **Step 4: Implémenter les models**

```python
# backend/rules/models.py
from enum import Enum
from typing import Union
from pydantic import BaseModel, field_validator

class ConditionField(str, Enum):
    DAYS_NOT_WATCHED = "days_not_watched"
    PLAY_COUNT       = "play_count"
    RATING           = "rating"
    FILE_SIZE_GB     = "file_size_gb"
    ADDED_DAYS_AGO   = "added_days_ago"
    MEDIA_TYPE       = "media_type"       # "Movie" | "Episode"
    SEERR_USER_ID    = "seerr_user_id"

class ConditionOp(str, Enum):
    GT     = "gt"
    LT     = "lt"
    GTE    = "gte"
    LTE    = "lte"
    EQ     = "eq"
    IN     = "in"
    NOT_IN = "not_in"

class RuleOperator(str, Enum):
    AND = "AND"
    OR  = "OR"

class RuleAction(str, Enum):
    QUEUE       = "queue"        # add to deletion queue
    NOTIFY_ONLY = "notify_only"  # Discord alert, no deletion

class Condition(BaseModel):
    field: ConditionField
    op:    ConditionOp
    value: Union[int, float, str, list]

    @field_validator("value")
    @classmethod
    def _validate_list_ops(cls, v, info):
        op = info.data.get("op")
        if op in (ConditionOp.IN, ConditionOp.NOT_IN) and not isinstance(v, list):
            raise ValueError(f"op={op} requires a list value, got {type(v).__name__}")
        return v

class ExpertRule(BaseModel):
    id:          int | None     = None
    name:        str
    library_id:  int | None     = None   # None = applies to all libraries
    conditions:  list[Condition]
    operator:    RuleOperator   = RuleOperator.AND
    action:      RuleAction     = RuleAction.QUEUE
    enabled:     bool           = True
    priority:    int            = 0      # lower = evaluated first
    created_at:  str | None     = None
```

- [ ] **Step 5: Vérifier que les tests passent**

```bash
python3 -m pytest tests/test_rules_models.py -v
```
Attendu : 6 passed

- [ ] **Step 6: Commit**

```bash
git add backend/rules/ tests/test_rules_models.py
git commit -m "feat: add Pydantic models for expert rules (Condition, ExpertRule)"
```

---

## Task 2 — Moteur d'évaluation des règles

**Files:**
- Create: `backend/rules/engine.py`
- Create: `tests/test_rules_engine.py`

- [ ] **Step 1: Écrire les tests d'abord**

```python
# tests/test_rules_engine.py
import pytest
from backend.rules.models import (
    Condition, ExpertRule, RuleAction, RuleOperator,
    ConditionField, ConditionOp,
)
from backend.rules.engine import evaluate_condition, evaluate_rule

MOVIE = {
    "days_not_watched": 400,
    "play_count": 1,
    "rating": 4.5,
    "file_size_gb": 25.0,
    "added_days_ago": 500,
    "media_type": "Movie",
    "seerr_user_id": 42,
}

@pytest.mark.parametrize("op,value,expected", [
    (ConditionOp.GT, 365, True),
    (ConditionOp.GT, 500, False),
    (ConditionOp.LT, 500, True),
    (ConditionOp.LT, 365, False),
    (ConditionOp.GTE, 400, True),
    (ConditionOp.LTE, 400, True),
    (ConditionOp.EQ, 400, True),
    (ConditionOp.EQ, 399, False),
])
def test_evaluate_condition_numeric(op, value, expected):
    c = Condition(field=ConditionField.DAYS_NOT_WATCHED, op=op, value=value)
    assert evaluate_condition(c, MOVIE) == expected

def test_evaluate_condition_in():
    c = Condition(field=ConditionField.SEERR_USER_ID, op=ConditionOp.IN, value=[42, 99])
    assert evaluate_condition(c, MOVIE) is True
    c2 = Condition(field=ConditionField.SEERR_USER_ID, op=ConditionOp.IN, value=[1, 2])
    assert evaluate_condition(c2, MOVIE) is False

def test_evaluate_condition_not_in():
    c = Condition(field=ConditionField.SEERR_USER_ID, op=ConditionOp.NOT_IN, value=[1, 2])
    assert evaluate_condition(c, MOVIE) is True

def test_evaluate_condition_media_type_eq():
    c = Condition(field=ConditionField.MEDIA_TYPE, op=ConditionOp.EQ, value="Movie")
    assert evaluate_condition(c, MOVIE) is True

def test_evaluate_condition_missing_field_returns_false():
    c = Condition(field=ConditionField.RATING, op=ConditionOp.GT, value=3.0)
    assert evaluate_condition(c, {}) is False  # field absent = condition fails

def test_evaluate_rule_and_all_match():
    rule = ExpertRule(name="r", conditions=[
        Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=365),
        Condition(field=ConditionField.RATING, op=ConditionOp.LT, value=5.0),
    ], operator=RuleOperator.AND)
    assert evaluate_rule(rule, MOVIE) is True

def test_evaluate_rule_and_partial_match():
    rule = ExpertRule(name="r", conditions=[
        Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=365),
        Condition(field=ConditionField.RATING, op=ConditionOp.GT, value=8.0),  # fails (4.5 < 8.0)
    ], operator=RuleOperator.AND)
    assert evaluate_rule(rule, MOVIE) is False

def test_evaluate_rule_or_partial_match():
    rule = ExpertRule(name="r", conditions=[
        Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=365),
        Condition(field=ConditionField.RATING, op=ConditionOp.GT, value=8.0),  # fails
    ], operator=RuleOperator.OR)
    assert evaluate_rule(rule, MOVIE) is True  # first condition passes → OR = True

def test_evaluate_rule_disabled_always_false():
    rule = ExpertRule(name="r", enabled=False, conditions=[
        Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=1),
    ])
    assert evaluate_rule(rule, MOVIE) is False

def test_evaluate_rule_no_conditions_returns_false():
    rule = ExpertRule(name="r", conditions=[])
    assert evaluate_rule(rule, MOVIE) is False
```

- [ ] **Step 2: Vérifier que les tests échouent**

```bash
python3 -m pytest tests/test_rules_engine.py -v
```
Attendu : `ImportError`

- [ ] **Step 3: Implémenter le moteur**

```python
# backend/rules/engine.py
import logging
from .models import Condition, ConditionOp, ExpertRule, RuleOperator

logger = logging.getLogger(__name__)

_NUMERIC_OPS = {
    ConditionOp.GT:  lambda a, b: a > b,
    ConditionOp.LT:  lambda a, b: a < b,
    ConditionOp.GTE: lambda a, b: a >= b,
    ConditionOp.LTE: lambda a, b: a <= b,
    ConditionOp.EQ:  lambda a, b: a == b,
}

def evaluate_condition(condition: Condition, item: dict) -> bool:
    """Return True if item satisfies condition. Missing field = False."""
    raw = item.get(condition.field.value)
    if raw is None:
        return False
    try:
        op = condition.op
        val = condition.value
        if op in _NUMERIC_OPS:
            return _NUMERIC_OPS[op](raw, val)
        if op == ConditionOp.IN:
            return raw in val
        if op == ConditionOp.NOT_IN:
            return raw not in val
    except Exception as e:
        logger.debug("evaluate_condition(%s): %s", condition.field, e)
    return False


def evaluate_rule(rule: ExpertRule, item: dict) -> bool:
    """Return True if item matches rule. Disabled rules always return False."""
    if not rule.enabled or not rule.conditions:
        return False
    results = [evaluate_condition(c, item) for c in rule.conditions]
    if rule.operator == RuleOperator.AND:
        return all(results)
    return any(results)  # OR
```

- [ ] **Step 4: Vérifier que les tests passent**

```bash
python3 -m pytest tests/test_rules_engine.py -v
```
Attendu : 12 passed

- [ ] **Step 5: Suite complète**

```bash
python3 -m pytest -q
```
Attendu : tous les tests passent

- [ ] **Step 6: Commit**

```bash
git add backend/rules/engine.py tests/test_rules_engine.py
git commit -m "feat: add expert rule evaluation engine"
```

---

## Task 3 — DB schema pour les règles expertes

**Files:**
- Modify: `backend/db/schema.py` (ajouter table `expert_rules`)
- Modify: `backend/db/repositories.py` (CRUD expert_rules)
- Modify: `backend/db/__init__.py` (exports)
- Create: `tests/test_expert_rules_repo.py`

- [ ] **Step 1: Écrire les tests d'abord**

```python
# tests/test_expert_rules_repo.py
import pytest
import pytest_asyncio
from backend.db.schema import init_db
from backend.db.repositories import (
    save_expert_rule, get_expert_rules, delete_expert_rule,
    get_expert_rule_by_id,
)
from backend.rules.models import (
    ExpertRule, Condition, ConditionField, ConditionOp, RuleOperator, RuleAction,
)

_RULE = ExpertRule(
    name="Test rule",
    conditions=[Condition(field=ConditionField.DAYS_NOT_WATCHED, op=ConditionOp.GT, value=365)],
)

@pytest_asyncio.fixture
async def db_path(monkeypatch, tmp_path):
    path = str(tmp_path / "test.db")
    monkeypatch.setattr("backend.db.utils.DB_PATH", path)
    monkeypatch.setattr("backend.db.schema.DB_PATH", path)
    monkeypatch.setattr("backend.db.repositories.DB_PATH", path)
    await init_db()
    return path

@pytest.mark.asyncio
async def test_save_and_get_expert_rules(db_path):
    await save_expert_rule(_RULE, db_path=db_path)
    rules = await get_expert_rules(db_path=db_path)
    assert len(rules) == 1
    assert rules[0].name == "Test rule"
    assert len(rules[0].conditions) == 1

@pytest.mark.asyncio
async def test_save_returns_id(db_path):
    rule_id = await save_expert_rule(_RULE, db_path=db_path)
    assert isinstance(rule_id, int) and rule_id > 0

@pytest.mark.asyncio
async def test_update_expert_rule(db_path):
    rule_id = await save_expert_rule(_RULE, db_path=db_path)
    updated = _RULE.model_copy(update={"id": rule_id, "name": "Updated"})
    await save_expert_rule(updated, db_path=db_path)
    rules = await get_expert_rules(db_path=db_path)
    assert len(rules) == 1
    assert rules[0].name == "Updated"

@pytest.mark.asyncio
async def test_delete_expert_rule(db_path):
    rule_id = await save_expert_rule(_RULE, db_path=db_path)
    await delete_expert_rule(rule_id, db_path=db_path)
    rules = await get_expert_rules(db_path=db_path)
    assert len(rules) == 0

@pytest.mark.asyncio
async def test_get_by_id(db_path):
    rule_id = await save_expert_rule(_RULE, db_path=db_path)
    rule = await get_expert_rule_by_id(rule_id, db_path=db_path)
    assert rule is not None
    assert rule.id == rule_id

@pytest.mark.asyncio
async def test_get_by_id_not_found(db_path):
    rule = await get_expert_rule_by_id(999, db_path=db_path)
    assert rule is None

@pytest.mark.asyncio
async def test_only_enabled_by_default(db_path):
    await save_expert_rule(_RULE, db_path=db_path)
    disabled = _RULE.model_copy(update={"name": "Disabled", "enabled": False})
    await save_expert_rule(disabled, db_path=db_path)
    rules = await get_expert_rules(db_path=db_path, enabled_only=True)
    assert len(rules) == 1
    assert rules[0].name == "Test rule"
```

- [ ] **Step 2: Vérifier que les tests échouent**

```bash
python3 -m pytest tests/test_expert_rules_repo.py -v
```
Attendu : erreurs d'import ou de table manquante

- [ ] **Step 3: Ajouter la table dans schema.py**

Trouver la dernière migration dans `backend/db/schema.py` (chercher le bloc `CREATE TABLE IF NOT EXISTS` le plus récent). Ajouter après les tables existantes :

```python
await db.execute("""
    CREATE TABLE IF NOT EXISTS expert_rules (
        id          INTEGER PRIMARY KEY AUTOINCREMENT,
        name        TEXT    NOT NULL,
        library_id  INTEGER,
        conditions  TEXT    NOT NULL DEFAULT '[]',
        operator    TEXT    NOT NULL DEFAULT 'AND',
        action      TEXT    NOT NULL DEFAULT 'queue',
        enabled     INTEGER NOT NULL DEFAULT 1,
        priority    INTEGER NOT NULL DEFAULT 0,
        created_at  TEXT    NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ','now'))
    )
""")
```

- [ ] **Step 4: Ajouter les fonctions dans repositories.py**

```python
# Ajouter à backend/db/repositories.py — importer ExpertRule en haut du fichier
# from ..rules.models import ExpertRule

async def save_expert_rule(rule: "ExpertRule", *, db_path: str) -> int:
    """Insert or update an expert rule. Returns the rule id."""
    import json
    conditions_json = json.dumps([c.model_dump() for c in rule.conditions])
    async with aiosqlite.connect(db_path) as db:
        if rule.id:
            await db.execute(
                "UPDATE expert_rules SET name=?, library_id=?, conditions=?, operator=?, "
                "action=?, enabled=?, priority=? WHERE id=?",
                (rule.name, rule.library_id, conditions_json, rule.operator.value,
                 rule.action.value, int(rule.enabled), rule.priority, rule.id),
            )
            await db.commit()
            return rule.id
        cursor = await db.execute(
            "INSERT INTO expert_rules (name, library_id, conditions, operator, action, enabled, priority) "
            "VALUES (?, ?, ?, ?, ?, ?, ?)",
            (rule.name, rule.library_id, conditions_json, rule.operator.value,
             rule.action.value, int(rule.enabled), rule.priority),
        )
        await db.commit()
        return cursor.lastrowid


async def get_expert_rules(*, db_path: str, enabled_only: bool = False) -> list["ExpertRule"]:
    """Return all expert rules, ordered by priority then id."""
    import json
    from ..rules.models import ExpertRule, Condition, RuleOperator, RuleAction
    where = "WHERE enabled=1" if enabled_only else ""
    async with aiosqlite.connect(db_path) as db:
        db.row_factory = aiosqlite.Row
        async with db.execute(
            f"SELECT * FROM expert_rules {where} ORDER BY priority ASC, id ASC"
        ) as cur:
            rows = await cur.fetchall()
    result = []
    for row in rows:
        d = dict(row)
        conditions = [Condition(**c) for c in json.loads(d.get("conditions") or "[]")]
        result.append(ExpertRule(
            id=d["id"], name=d["name"], library_id=d.get("library_id"),
            conditions=conditions,
            operator=RuleOperator(d["operator"]),
            action=RuleAction(d["action"]),
            enabled=bool(d["enabled"]),
            priority=d["priority"],
            created_at=d.get("created_at"),
        ))
    return result


async def get_expert_rule_by_id(rule_id: int, *, db_path: str) -> "ExpertRule | None":
    rules = await get_expert_rules(db_path=db_path)
    return next((r for r in rules if r.id == rule_id), None)


async def delete_expert_rule(rule_id: int, *, db_path: str) -> None:
    async with aiosqlite.connect(db_path) as db:
        await db.execute("DELETE FROM expert_rules WHERE id=?", (rule_id,))
        await db.commit()
```

- [ ] **Step 5: Ajouter les exports dans `backend/db/__init__.py`**

Ajouter dans l'import de repositories :
```python
from .repositories import (
    # ... existing ...
    save_expert_rule,
    get_expert_rules,
    get_expert_rule_by_id,
    delete_expert_rule,
)
```

Et dans `__all__` :
```python
"save_expert_rule", "get_expert_rules", "get_expert_rule_by_id", "delete_expert_rule",
```

- [ ] **Step 6: Vérifier que les tests passent**

```bash
python3 -m pytest tests/test_expert_rules_repo.py -v
```
Attendu : 7 passed

- [ ] **Step 7: Suite complète**

```bash
python3 -m pytest -q
```

- [ ] **Step 8: Commit**

```bash
git add backend/db/schema.py backend/db/repositories.py backend/db/__init__.py tests/test_expert_rules_repo.py
git commit -m "feat: add expert_rules table schema and repository CRUD"
```

---

## Task 4 — API endpoints CRUD + preview

**Files:**
- Create: `backend/routers/expert_rules.py`
- Modify: `backend/main.py` (include_router)
- Create: `tests/test_expert_rules_api.py`

- [ ] **Step 1: Écrire les tests d'abord**

```python
# tests/test_expert_rules_api.py
import pytest
from httpx import AsyncClient, ASGITransport
from backend.main import app
from backend.db.utils import DB_PATH
from backend.db.schema import init_db

@pytest.fixture(autouse=True)
def patch_db(monkeypatch, tmp_path):
    path = str(tmp_path / "test.db")
    monkeypatch.setattr("backend.db.utils.DB_PATH", path)
    monkeypatch.setattr("backend.db.schema.DB_PATH", path)
    monkeypatch.setattr("backend.db.repositories.DB_PATH", path)
    monkeypatch.setattr("backend.main.DB_PATH", path)
    import asyncio
    asyncio.get_event_loop().run_until_complete(init_db())

@pytest.fixture
def auth_headers(monkeypatch):
    monkeypatch.setattr("backend.auth.verify_token", lambda t: t == "test-token")
    return {"Authorization": "Bearer test-token"}

_PAYLOAD = {
    "name": "Old movies",
    "conditions": [{"field": "days_not_watched", "op": "gt", "value": 365}],
    "operator": "AND",
    "action": "queue",
    "enabled": True,
    "priority": 0,
}

@pytest.mark.asyncio
async def test_create_expert_rule(auth_headers):
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as c:
        r = await c.post("/api/expert-rules", json=_PAYLOAD, headers=auth_headers)
    assert r.status_code == 201
    data = r.json()
    assert data["id"] > 0
    assert data["name"] == "Old movies"

@pytest.mark.asyncio
async def test_list_expert_rules(auth_headers):
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as c:
        await c.post("/api/expert-rules", json=_PAYLOAD, headers=auth_headers)
        r = await c.get("/api/expert-rules", headers=auth_headers)
    assert r.status_code == 200
    assert len(r.json()) == 1

@pytest.mark.asyncio
async def test_delete_expert_rule(auth_headers):
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http/test") as c:
        create = await c.post("/api/expert-rules", json=_PAYLOAD, headers=auth_headers)
        rule_id = create.json()["id"]
        r = await c.delete(f"/api/expert-rules/{rule_id}", headers=auth_headers)
        assert r.status_code == 204
        list_r = await c.get("/api/expert-rules", headers=auth_headers)
        assert len(list_r.json()) == 0

@pytest.mark.asyncio
async def test_unauthorized_returns_401():
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as c:
        r = await c.get("/api/expert-rules")
    assert r.status_code == 401
```

- [ ] **Step 2: Vérifier que les tests échouent**

```bash
python3 -m pytest tests/test_expert_rules_api.py -v
```
Attendu : 404 (route inexistante)

- [ ] **Step 3: Implémenter le router**

```python
# backend/routers/expert_rules.py
from fastapi import APIRouter, Depends, HTTPException
from fastapi.responses import Response

from ..auth import require_auth
from ..db.utils import DB_PATH
from ..db.repositories import (
    save_expert_rule, get_expert_rules, get_expert_rule_by_id, delete_expert_rule,
)
from ..rules.models import ExpertRule

router = APIRouter(prefix="/api/expert-rules", tags=["expert-rules"])


@router.get("")
async def list_expert_rules(user: str = Depends(require_auth)):
    rules = await get_expert_rules(db_path=DB_PATH)
    return [r.model_dump() for r in rules]


@router.post("", status_code=201)
async def create_expert_rule(rule: ExpertRule, user: str = Depends(require_auth)):
    rule.id = None  # force INSERT
    rule_id = await save_expert_rule(rule, db_path=DB_PATH)
    created = await get_expert_rule_by_id(rule_id, db_path=DB_PATH)
    return created.model_dump()


@router.put("/{rule_id}")
async def update_expert_rule(rule_id: int, rule: ExpertRule, user: str = Depends(require_auth)):
    existing = await get_expert_rule_by_id(rule_id, db_path=DB_PATH)
    if not existing:
        raise HTTPException(status_code=404, detail="Rule not found")
    rule.id = rule_id
    await save_expert_rule(rule, db_path=DB_PATH)
    return (await get_expert_rule_by_id(rule_id, db_path=DB_PATH)).model_dump()


@router.delete("/{rule_id}", status_code=204)
async def delete_expert_rule_endpoint(rule_id: int, user: str = Depends(require_auth)):
    existing = await get_expert_rule_by_id(rule_id, db_path=DB_PATH)
    if not existing:
        raise HTTPException(status_code=404, detail="Rule not found")
    await delete_expert_rule(rule_id, db_path=DB_PATH)
    return Response(status_code=204)
```

- [ ] **Step 4: Enregistrer le router dans main.py**

Dans `backend/main.py`, importer et inclure :
```python
from .routers import expert_rules
# ...
app.include_router(expert_rules.router)
```

- [ ] **Step 5: Vérifier que les tests passent**

```bash
python3 -m pytest tests/test_expert_rules_api.py -v
python3 -m pytest -q
```

- [ ] **Step 6: Commit**

```bash
git add backend/routers/expert_rules.py backend/main.py tests/test_expert_rules_api.py
git commit -m "feat: add /api/expert-rules CRUD endpoints"
```

---

## Task 5 — Intégration dans le scanner

**Files:**
- Modify: `backend/scanner.py` (évaluer les règles expertes pour chaque item)
- Create: `tests/test_scan_integration.py`

Le scanner reçoit déjà les informations de chaque item Emby. Après avoir vérifié les règles simples existantes, il doit :
1. Charger les règles expertes activées pour la bibliothèque
2. Pour chaque item, évaluer les règles dans l'ordre de priorité
3. Première règle qui matche détermine l'action : `queue` ou `notify_only`

- [ ] **Step 1: Écrire le test d'intégration**

```python
# tests/test_scan_integration.py
"""Integration tests for the scan pipeline — uses real SQLite, mocks Emby client."""
import pytest
import pytest_asyncio
import aiosqlite
from unittest.mock import AsyncMock, patch
from backend.db.schema import init_db
from backend.db.repositories import save_expert_rule, get_expert_rules
from backend.rules.models import ExpertRule, Condition, ConditionField, ConditionOp, RuleAction

_EMBY_ITEM = {
    "Id": "emby-001",
    "Name": "The Batman",
    "Type": "Movie",
    "UserData": {"LastPlayedDate": None, "PlayCount": 0},
    "MediaSources": [{"Size": 25_000_000_000}],
    "CommunityRating": 7.5,
    "DateCreated": "2021-01-01T00:00:00Z",
}

@pytest_asyncio.fixture
async def db_path(monkeypatch, tmp_path):
    path = str(tmp_path / "scan_test.db")
    monkeypatch.setattr("backend.db.utils.DB_PATH", path)
    monkeypatch.setattr("backend.db.schema.DB_PATH", path)
    monkeypatch.setattr("backend.db.repositories.DB_PATH", path)
    monkeypatch.setattr("backend.scanner.DB_PATH", path)
    await init_db()
    # Insert a library
    async with aiosqlite.connect(path) as db:
        await db.execute(
            "INSERT INTO libraries (id, name, emby_id, server_id, enabled) VALUES (1,'Films','L1','0',1)"
        )
        await db.execute(
            "INSERT INTO media_servers (id, url, api_key, user_id) VALUES ('0','http://emby','key','uid')"
        )
        await db.commit()
    return path

@pytest.mark.asyncio
async def test_expert_rule_queues_unwatched(db_path):
    """An expert rule with action=queue should add the item to media_queue."""
    rule = ExpertRule(
        name="Never watched",
        conditions=[Condition(field=ConditionField.PLAY_COUNT, op=ConditionOp.EQ, value=0)],
        action=RuleAction.QUEUE,
    )
    await save_expert_rule(rule, db_path=db_path)

    with patch("backend.scanner.get_client") as mock_get_client, \
         patch("backend.scanner.get_media_servers") as mock_servers:
        mock_servers.return_value = [{"id": "0", "url": "http://emby", "api_key": "key", "user_id": "uid"}]
        mock_client = AsyncMock()
        mock_client.get_libraries.return_value = [{"ItemId": "L1", "Name": "Films"}]
        mock_client.get_items.return_value = [_EMBY_ITEM]
        mock_client.get_play_state.return_value = {"PlayCount": 0, "LastPlayedDate": None}
        mock_get_client.return_value = mock_client
        from backend.scanner import run_scan
        await run_scan()

    async with aiosqlite.connect(db_path) as db:
        async with db.execute("SELECT COUNT(*) FROM media_queue WHERE emby_id='emby-001'") as cur:
            count = (await cur.fetchone())[0]
    assert count == 1
```

- [ ] **Step 2: Vérifier que le test échoue avant implémentation**

```bash
python3 -m pytest tests/test_scan_integration.py -v
```

- [ ] **Step 3: Intégrer dans scanner.py**

Localiser dans `backend/scanner.py` la fonction `run_scan` ou `_process_item` qui décide si un item doit être mis en queue.

Ajouter après les vérifications existantes :

```python
# Dans scanner.py — au niveau de l'import
from .db.repositories import get_expert_rules as _get_expert_rules
from .rules.engine import evaluate_rule as _evaluate_rule
from .rules.models import RuleAction as _RuleAction

# Lors du traitement de chaque item, après les règles simples :
async def _evaluate_expert_rules(item_data: dict, library_id: int | None) -> str | None:
    """Return action string ('queue'/'notify_only') if any expert rule matches, else None."""
    rules = await _get_expert_rules(db_path=DB_PATH, enabled_only=True)
    for rule in rules:
        if rule.library_id and rule.library_id != library_id:
            continue
        if _evaluate_rule(rule, item_data):
            return rule.action.value
    return None
```

Appeler `_evaluate_expert_rules` dans le pipeline de scan pour chaque item et agir selon l'action retournée.

Note : consulter `backend/scanner.py` pour identifier le bon point d'insertion dans `_process_item` ou `_check_item` — la logique diffère selon la structure actuelle. Insérer APRÈS les règles simples, AVANT l'écriture en queue.

- [ ] **Step 4: Vérifier que le test d'intégration passe**

```bash
python3 -m pytest tests/test_scan_integration.py -v
python3 -m pytest -q
```

- [ ] **Step 5: Commit**

```bash
git add backend/scanner.py tests/test_scan_integration.py
git commit -m "feat: integrate expert rules evaluation into scan pipeline"
```

---

## Task 6 — Normalisation du schéma media_queue

**Files:**
- Modify: `backend/db/schema.py` (migration: colonnes notified_* → table notifications)
- Modify: `backend/notifications.py` et partout où `notified_*` est référencé
- Create: `tests/test_schema_migration.py`

**Objectif :** Remplacer les colonnes `notified_30d`, `notified_7d`, `notified_1d` (et variantes futures) par une table `notifications` séparée.

- [ ] **Step 1: Inventaire des colonnes actuelles**

```bash
cd /opt/claude/hygie && python3 -c "
import sqlite3, json
conn = sqlite3.connect('data/hygie.db' if __import__('os').path.exists('data/hygie.db') else ':memory:')
conn.execute('''CREATE TABLE IF NOT EXISTS media_queue (
    id INTEGER PRIMARY KEY, emby_id TEXT, notified_30d INTEGER DEFAULT 0,
    notified_7d INTEGER DEFAULT 0, notified_1d INTEGER DEFAULT 0
)''')
cols = [row[1] for row in conn.execute('PRAGMA table_info(media_queue)')]
print([c for c in cols if 'notified' in c.lower()])
"
```

Lister toutes les colonnes `notified_*` et les fichiers qui les référencent :

```bash
grep -rn "notified_" backend/ --include="*.py" | grep -v ".pyc"
```

- [ ] **Step 2: Créer la table notifications**

Ajouter dans `backend/db/schema.py` :

```python
await db.execute("""
    CREATE TABLE IF NOT EXISTS notifications (
        id          INTEGER PRIMARY KEY AUTOINCREMENT,
        media_id    INTEGER NOT NULL REFERENCES media_queue(id) ON DELETE CASCADE,
        threshold   TEXT    NOT NULL,   -- '30d', '7d', '1d', 'now', custom key
        sent_at     TEXT    NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ','now'))
    )
""")
await db.execute("CREATE INDEX IF NOT EXISTS idx_notif_media ON notifications(media_id)")
```

- [ ] **Step 3: Écrire le test de migration**

```python
# tests/test_schema_migration.py
import pytest
import pytest_asyncio
import aiosqlite
from backend.db.schema import init_db

@pytest_asyncio.fixture
async def db_path(monkeypatch, tmp_path):
    path = str(tmp_path / "migration.db")
    monkeypatch.setattr("backend.db.schema.DB_PATH", path)
    monkeypatch.setattr("backend.db.utils.DB_PATH", path)
    await init_db()
    return path

@pytest.mark.asyncio
async def test_notifications_table_created(db_path):
    async with aiosqlite.connect(db_path) as db:
        async with db.execute(
            "SELECT name FROM sqlite_master WHERE type='table' AND name='notifications'"
        ) as cur:
            row = await cur.fetchone()
    assert row is not None

@pytest.mark.asyncio
async def test_expert_rules_table_created(db_path):
    async with aiosqlite.connect(db_path) as db:
        async with db.execute(
            "SELECT name FROM sqlite_master WHERE type='table' AND name='expert_rules'"
        ) as cur:
            row = await cur.fetchone()
    assert row is not None
```

- [ ] **Step 4: Migrer les références `notified_*`**

Dans `backend/notifications.py` et `backend/db/repositories.py`, remplacer les lectures/écritures de `notified_*` par des insertions/requêtes dans la table `notifications`.

Pattern avant :
```python
await db.execute("UPDATE media_queue SET notified_30d=1 WHERE id=?", (item_id,))
```

Pattern après :
```python
await db.execute(
    "INSERT OR IGNORE INTO notifications (media_id, threshold) VALUES (?,?)",
    (item_id, "30d")
)
```

Check si déjà notifié :
```python
async with db.execute(
    "SELECT 1 FROM notifications WHERE media_id=? AND threshold=?", (item_id, "30d")
) as cur:
    already_sent = await cur.fetchone() is not None
```

- [ ] **Step 5: Tests**

```bash
python3 -m pytest tests/test_schema_migration.py tests/test_repositories.py -v
python3 -m pytest -q
```

- [ ] **Step 6: Commit**

```bash
git add backend/db/schema.py backend/notifications.py backend/db/repositories.py tests/test_schema_migration.py
git commit -m "refactor: replace notified_* columns with notifications table"
```

---

## Task 7 — Métriques par bibliothèque + corrélation run_id

**Files:**
- Modify: `backend/routers/metrics.py` (ajouter breakdown par bibliothèque)
- Modify: `backend/db/schema.py` (ajouter colonne `library_id` à stats_history si absente)
- Modify: `backend/deletion.py` (enregistrer `library_id` dans stats_history)
- Create: `tests/test_metrics.py`

**Objectif :** `/api/metrics` retourne des stats par bibliothèque, et chaque scan/deletion log corrèle avec un `run_id`.

- [ ] **Step 1: Vérifier la structure actuelle**

```bash
python3 -c "
import sqlite3
conn = sqlite3.connect(':memory:')
# check what stats_history columns exist
"
grep -n "stats_history\|run_id" backend/routers/metrics.py backend/deletion.py backend/scanner.py
```

- [ ] **Step 2: Écrire le test**

```python
# tests/test_metrics.py
import pytest
import aiosqlite
import pytest_asyncio
from httpx import AsyncClient, ASGITransport
from backend.main import app
from backend.db.schema import init_db

@pytest_asyncio.fixture
async def db_path(monkeypatch, tmp_path):
    path = str(tmp_path / "metrics.db")
    for mod in ["backend.db.utils", "backend.db.schema", "backend.db.repositories", "backend.main"]:
        monkeypatch.setattr(f"{mod}.DB_PATH", path)
    await init_db()
    # Seed stats
    async with aiosqlite.connect(path) as db:
        await db.executemany(
            "INSERT INTO stats_history (ts, total_deleted, total_scanned, space_freed_bytes, month, library_id) "
            "VALUES (?, ?, ?, ?, ?, ?)",
            [
                ("2026-05-01T10:00:00Z", 3, 100, 10_000_000_000, "2026-05", 1),
                ("2026-05-02T10:00:00Z", 1, 50, 5_000_000_000, "2026-05", 2),
            ]
        )
        await db.commit()
    return path

@pytest.fixture
def auth(monkeypatch):
    monkeypatch.setattr("backend.auth.verify_token", lambda t: t == "tok")
    return {"Authorization": "Bearer tok"}

@pytest.mark.asyncio
async def test_metrics_by_library(db_path, auth):
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as c:
        r = await c.get("/api/metrics", headers=auth)
    assert r.status_code == 200
    data = r.json()
    assert "by_library" in data
```

- [ ] **Step 3: Ajouter `library_id` dans stats_history**

Dans `backend/db/schema.py`, lors de la création de `stats_history` :
```sql
library_id INTEGER
```

Ajouter une migration ALTER TABLE si la colonne est absente :
```python
try:
    await db.execute("ALTER TABLE stats_history ADD COLUMN library_id INTEGER")
    await db.commit()
except Exception:
    pass  # column already exists
```

- [ ] **Step 4: Modifier `/api/metrics` pour retourner un breakdown**

Dans `backend/routers/metrics.py`, ajouter une requête :
```sql
SELECT library_id, SUM(total_deleted), SUM(space_freed_bytes)
FROM stats_history
WHERE month = ?
GROUP BY library_id
```

Retourner dans la réponse sous la clé `by_library: [{library_id, deleted, space_freed_bytes}]`.

- [ ] **Step 5: Tests**

```bash
python3 -m pytest tests/test_metrics.py -v
python3 -m pytest -q
```

- [ ] **Step 6: Commit**

```bash
git add backend/routers/metrics.py backend/db/schema.py backend/deletion.py tests/test_metrics.py
git commit -m "feat: add per-library breakdown to /api/metrics"
```

---

## Task 8 — Tests d'intégration deletion

**Files:**
- Create: `tests/test_deletion_integration.py`

- [ ] **Step 1: Écrire les tests**

```python
# tests/test_deletion_integration.py
"""Integration tests for run_deletion — real SQLite, mocked external services."""
import pytest
import pytest_asyncio
import aiosqlite
from unittest.mock import AsyncMock, patch
from backend.db.schema import init_db
from backend.db.utils import STATUS_PENDING

_QUEUE_ITEM = {
    "emby_id": "EMB-001", "title": "The Batman", "media_type": "Movie",
    "delete_at": "2020-01-01T00:00:00+00:00",  # in the past = ready to delete
    "status": STATUS_PENDING,
    "library_id": None, "file_path": "/movies/batman.mkv",
}

@pytest_asyncio.fixture
async def db_path(monkeypatch, tmp_path):
    path = str(tmp_path / "deletion.db")
    for mod in ["backend.db.utils", "backend.db.schema", "backend.db.repositories",
                "backend.deletion", "backend.scheduler"]:
        monkeypatch.setattr(f"{mod}.DB_PATH", path, raising=False)
    await init_db()
    async with aiosqlite.connect(path) as db:
        await db.execute(
            "INSERT INTO media_queue (emby_id, title, media_type, delete_at, status, file_path) "
            "VALUES (:emby_id, :title, :media_type, :delete_at, :status, :file_path)",
            _QUEUE_ITEM,
        )
        await db.commit()
    return path

@pytest.mark.asyncio
async def test_deletion_marks_item_deleted(db_path):
    with patch("backend.deletion.delete_item", new_callable=AsyncMock) as mock_del, \
         patch("backend.deletion.send_notification", new_callable=AsyncMock), \
         patch("backend.deletion.radarr_find_by_path", new_callable=AsyncMock, return_value=None), \
         patch("backend.deletion.qbit_find_by_path", new_callable=AsyncMock, return_value=None), \
         patch("backend.deletion.get_bool_setting", new_callable=AsyncMock, return_value=False), \
         patch("backend.deletion.get_setting", new_callable=AsyncMock, return_value="tag_only"):
        from backend.deletion import run_deletion
        await run_deletion()

    async with aiosqlite.connect(db_path) as db:
        async with db.execute(
            "SELECT status FROM media_queue WHERE emby_id='EMB-001'"
        ) as cur:
            row = await cur.fetchone()
    assert row[0] == "deleted"

@pytest.mark.asyncio
async def test_dry_run_does_not_delete(db_path):
    with patch("backend.deletion.get_bool_setting", new_callable=AsyncMock, return_value=True), \
         patch("backend.deletion.delete_item", new_callable=AsyncMock) as mock_del, \
         patch("backend.deletion.send_notification", new_callable=AsyncMock), \
         patch("backend.deletion.get_setting", new_callable=AsyncMock, return_value="tag_only"):
        from backend.deletion import run_deletion
        await run_deletion()
        mock_del.assert_not_called()
```

- [ ] **Step 2: Vérifier que les tests passent**

```bash
python3 -m pytest tests/test_deletion_integration.py -v
python3 -m pytest -q
```

- [ ] **Step 3: Commit**

```bash
git add tests/test_deletion_integration.py
git commit -m "test: add integration tests for run_deletion pipeline"
```

---

## Task 9 — Version bump + tag v2.8.0

**Files:**
- Modify: `backend/version.py`

- [ ] **Step 1: Suite complète**

```bash
python3 -m pytest -q
```
Attendu : tous les tests passent

- [ ] **Step 2: Bump version**

```python
# backend/version.py
VERSION = os.environ.get("HYGIE_VERSION", "2.8.0")
```

- [ ] **Step 3: Commit, push main, tag**

```bash
git add backend/version.py
git commit -m "chore: bump version to 2.8.0"
git push origin main
git tag v2.8.0
git push origin v2.8.0
```

---

## Résumé des tests attendus en fin de v2.8

| Fichier | Tests |
|---------|-------|
| test_rules_models.py | 6 |
| test_rules_engine.py | 12 |
| test_expert_rules_repo.py | 7 |
| test_expert_rules_api.py | 4 |
| test_scan_integration.py | 1 |
| test_deletion_integration.py | 2 |
| test_schema_migration.py | 2 |
| test_metrics.py | 1 |
| Tests existants (206) | 206 |
| **Total estimé** | **~241** |
