"""Services layer — business logic between routers and repositories.

Current modules:
  (empty — service layer is being progressively introduced)

This package will host domain logic extracted from routers,
keeping HTTP concerns separate from business rules.
"""
